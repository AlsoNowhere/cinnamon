// export const resolveSides = function (points, trail) {
//   trail.forEach((part) => {
//     const w =
//       part.key === "right" || part.direction === "right" ? this.width : 0;
//     const h =
//       part.key === "bottom" || part.direction === "bottom" ? this.height : 0;
//     points.push({ resolvedPoint: { width: w, height: h } });
//   });
// };
