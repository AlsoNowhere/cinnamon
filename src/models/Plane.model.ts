export class Plane {
  x: number;
  y: number;
  z: number;
  d: number;

  constructor(x: number, y: number, z: number, d: number) {
    this.x = x;
    this.y = y;
    this.z = z;
    this.d = d;
  }
}
